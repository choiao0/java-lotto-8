# java-lotto-precourse

### 📎 기능 요구 사항
간단한 로또 발매기를 구현한다.
* 로또 번호의 숫자 범위는 1~45까지이다.
* 1개의 로또를 발행할 때 중복되지 않는 6개의 숫자를 뽑는다.
* 당첨 번호 추첨 시 중복되지 않는 숫자 6개와 보너스 번호 1개를 뽑는다.
* 당첨은 1등부터 5등까지 있다. 당첨 기준과 금액은 아래와 같다.
  * 1등: 6개 번호 일치 / 2,000,000,000원
  * 2등: 5개 번호 + 보너스 번호 일치 / 30,000,000원
  * 3등: 5개 번호 일치 / 1,500,000원
  * 4등: 4개 번호 일치 / 50,000원
  * 5등: 3개 번호 일치 / 5,000원
* 로또 구입 금액을 입력하면 구입 금액에 해당하는 만큼 로또를 발행해야 한다.
* 로또 1장의 가격은 1,000원이다.
* 당첨 번호와 보너스 번호를 입력받는다.
* 사용자가 구매한 로또 번호와 당첨 번호를 비교하여 당첨 내역 및 수익률을 출력하고 로또 게임을 종료한다.
* 사용자가 잘못된 값을 입력할 경우 IllegalArgumentException을 발생시키고, "[ERROR]"로 시작하는 에러 메시지를 출력 후 그 부분부터 입력을 다시 받는다.
  * Exception이 아닌 IllegalArgumentException, IllegalStateException 등과 같은 명확한 유형을 처리한다.

### ⚙️ 기능 목록 정리

- [ ]  로또 구입 금액을 입력 받는다.
- [ ]  로또 발행 수량을 계산한다.
- [ ]  수량만큼 로또를 발행한다.
    - [ ]  1개의 로또를 발행할 때 중복되지 않는 6개의 숫자를 뽑는다.
- [ ]  발행한 로또 수량 및 번호를 출력한다. 로또 번호는 오름차순으로 정렬하여 보여준다.
- [ ]  당첨 번호를 입력 받는다.
    - [ ]  번호는 쉼표(,)를 기준으로 구분한다.
- [ ]  보너스 번호를 입력 받는다.
- [ ]  사용자가 구매한 로또 번호와 당첨 번호를 비교한다.
    - [ ]  로또가 당첨되면 당첨 로또를 기록한다.
- [ ]  당첨 내역을 출력한다.
- [ ]  수익률을 계산한다.
- [ ]  수익률을 소수점 둘째 자리에서 반올림하여 출력한다.


### 🧩 예외 사항

사용자가 잘못된 값을 입력할 경우`IllegalArgumentException`을 발생시키고, "[ERROR]"로 시작하는 에러 메시지를 출력 후 그 부분부터 입력을 다시 받는다.

로또 구입 금액을 잘못 입력한 경우
- [ ]  금액이 1000 미만인 경우
- [ ]  금액에 숫자 외의 다른 문자가 있는 경우
- [ ]  금액이 1000으로 나누어 떨어지지 않는 경우

당첨 번호를 잘못 입력한 경우
- [ ]  당첨 번호가 공백인 경우
- [ ]  당첨 번호가 1~45 범위 밖인 경우
- [ ]  당첨 번호가 6개가 아닌 경우
- [ ]  당첨 번호에 숫자와 쉼표 외의 다른 문자가 있는 경우
- [ ]  당첨 번호에 중복된 숫자가 있는 경우

보너스 번호를 잘못 입력한 경우
- [ ]  보너스 번호가 공백인 경우
- [ ]  보너스 번호가 1~45 범위 밖인 경우
- [ ]  보너스 번호에 숫자 외의 다른 문자가 있는 경우

로또 번호가 잘못된 경우
- [ ]  로또 번호가 6개가 아닌 경우
- [ ]  로또 번호에 중복된 숫자가 있는 경우

### 📦 패키지 구조
```
src
├── main
│   └── java
│       └── lotto
│           ├── controller
│           │   └── LottoController.java        // 로또 게임 전체 흐름 제어
│           ├── domain
│           │   ├── BonusNumber.java            // 보너스 번호 관리
│           │   ├── Lotto.java                  // 단일 로또 번호 관리
│           │   ├── LottoQuantity.java          // 로또 구매 수량 관리
│           │   ├── Lottos.java                 // 구매한 로또 목록 관리 
│           │   ├── PurchaseAmount.java         // 구매 금액 관리
│           │   ├── WinningLotto.java           // 당첨 로또 관리
│           │   ├── WinningLottos.java          // 당첨 로또 목록 관리
│           │   ├── WinningNumbers.java         // 당첨 번호 관리
│           │   ├── WinningRank.java            // (enum) 당첨 등수 정의
│           │   └── WinningStatistics.java      // 당첨 통계 관리
│           ├── exception
│           │   └── ErrorCode.java              // (enum) 예외 코드 정의
│           ├── view
│           │   ├── LottoInput.java             // 사용자 입력 처리
│           │   └── LottoOutput.java            // 결과 출력 처리
│           └── Application.java
└── test
    └── java
        └── lotto
            ├── domain
            │   ├── BonusNumberTest.java        // 보너스 번호 검증 테스트
            │   ├── LottoQuantityTest.java      // 로또 구매 수량 계산 테스트
            │   ├── LottosTest.java             // 로또 목록 생성 테스트
            │   ├── LottoTest.java              // 로또 번호 유효성 검증 테스트
            │   ├── PurchaseAmountTest.java     // 구매 금액 검증 테스트
            │   ├── WinningNumbersTest.java     // 당첨 번호 검증 테스트
            │   └── WinningRankTest.java        // 당첨 등수 판별 테스트
            └── ApplicationTest.java
```